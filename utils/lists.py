def size(list):
    return len(list)

# + Operator to join 2 lists
# list.append(new_element)
# del(0) Remove first element (Element is the position)
# remove(element) element is the value of the element, not the position
# list.index(value) Return position
# list.count(value) Return the count of value existences